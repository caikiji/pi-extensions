{
  "name": "pi-extensions",
  "version": "0.16.0",
  "description": "Personal pi extensions: handoff, rules, skim, checkpoint",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "license": "MIT",
  "scripts": {
    "test": "node tests/run-all.mjs",
    "lint": "eslint ."
  },
  "pi": {
    "extensions": [
      "./extensions/handoff.ts",
      "./extensions/rules.ts",
      "./extensions/skim.ts",
      "./extensions/checkpoint.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-tui": "0.83.0",
    "eslint": "^9.39.5",
    "typescript-eslint": "^8.65.0"
  }
}
