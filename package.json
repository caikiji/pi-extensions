{
  "name": "pi-extensions",
  "version": "0.2.0",
  "description": "Personal pi extensions: handoff, rules",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "license": "MIT",
  "scripts": {
    "test": "node tests/run-all.mjs"
  },
  "pi": {
    "extensions": [
      "./extensions/handoff.ts",
      "./extensions/rules.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-coding-agent": "*"
  }
}
